import React from 'react'
import ReactDOM from 'react-dom/client'
import TourVirtuale from './tour' 

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TourVirtuale />
  </React.StrictMode>,
)